#include <string.h>
/**
 * _strcat - check the code
 * @dest: target location
 * @src: target location
 * Return: charecter.
 */
char *_strcat(char *dest, char *src)
{
strcat(dest, src);
return (dest);
}
