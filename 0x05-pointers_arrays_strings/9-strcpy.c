#include <stdio.h>
#include <string.h>
/**
 * _strcpy - check the code
 * @dest: destenation
 * @src: soyrce
 * Return: Always 0.
 */
char *_strcpy(char *dest, char *src)
{
strcpy(dest, src);
return (dest);
}
